<?xml version="1.0" encoding="utf-8"?>
<!--
   NexCore doesn't persist any user data; this rule file simply opts in
   to the default backup behaviour so we don't have to declare
   explicit include/exclude blocks.
-->
<full-backup-content />