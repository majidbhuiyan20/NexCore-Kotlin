package com.matox.nexcore.ui.theme

import androidx.compose.ui.graphics.Color

// --- Brand ---
val NexCoreGreen = Color(0xFF22C55E)
val NexCoreGreenAccent = Color(0xFF16A34A)
val NexCoreGreenDark = Color(0xFF15803D)
val NexCoreProBadge = Color(0xFF22C55E)

// --- Backgrounds ---
val Background = Color(0xFF0B1220)
val BackgroundGradientTop = Color(0xFF0E1729)
val BackgroundGradientBottom = Color(0xFF0A111F)
val Surface = Color(0xFF131C2F)
val SurfaceVariant = Color(0xFF1A2540)
val CardStroke = Color(0xFF1F2A44)

// --- Text ---
val TextPrimary = Color(0xFFE6EDF7)
val TextSecondary = Color(0xFF94A3B8)
val TextTertiary = Color(0xFF64748B)

// --- Metric / accent palette ---
val MetricBlue = Color(0xFF3B82F6)
val MetricCyan = Color(0xFF06B6D4)
val MetricPurple = Color(0xFFA855F7)
val MetricViolet = Color(0xFF8B5CF6)
val MetricGreen = Color(0xFF22C55E)
val MetricOrange = Color(0xFFF97316)
val MetricRed = Color(0xFFEF4444)
val MetricPink = Color(0xFFEC4899)
val MetricTeal = Color(0xFF14B8A6)

// --- Tracks ---
val TrackGray = Color(0xFF22304A)

// --- Health banner gradient ---
val HealthGreenStart = Color(0xFF22C55E)
val HealthGreenEnd = Color(0xFF15803D)
val HealthCardBackground = Color(0xFF0F2A1F)

// --- Glass / overlay ---
/** Subtle white overlay used for the glass shimmer on top of
 *  premium cards. Kept at low alpha (~20%) so it reads as a
 *  reflection, not a tinted surface. */
val GlassHighlight = Color(0x33FFFFFF)

/** Soft red — for "High Usage" badge text on a dark background.
 *  Brighter than MetricRed so it stays legible on the
 *  red-tinted badge surface. */
val MetricSoftRed = Color(0xFFFCA5A5)